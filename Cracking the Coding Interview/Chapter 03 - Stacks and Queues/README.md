# Ch3 Stacks and Queues
## Stack
- Last-In-First-Out
- `pop()` : Remove from top
- `push(item)` : Add to the top
- `peek()` : return top
- `isEmpty()` : Return true iff stack is empty

## Queue
- First-In-First-Out
- `add(item)` : add to the back
- `remove()` : remove first in the queue
- `peek()` : return top of queue
- `isEmpty()` : return true iff queue is empty
